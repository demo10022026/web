server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: "##HUYhallo08092005"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Ho_Chi_Minh
    default-property-inclusion: non_null

  mail:
    host: smtp.gmail.com
    port: 587
    username: demo10022026@gmail.com
    password: "wdbl outn vmnj deho"
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

app:
  jwt:
    secret: "b106eae7b91ad61d18ee2fc0fe45c74c5886f4727728cfeff4dfbb4e0df577e0"
    expiration-ms: 900000
    refresh-expiration-ms: 604800000

  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:3000
      - https://*.trycloudflare.com

  cloudinary:
    cloud-name: dnohbihzz
    api-key: "785526547428381"
    api-secret: IH57kn7-R3nJZ-l4SOvcSWbyKdk

  upload:
    local-dir: uploads            # thư mục lưu file, tạo tự động
    use-local: false               # true = local trước, false = Cloudinary trước
    base-url: http://localhost:8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha

logging:
  level:
    com.ecommerce: DEBUG
    org.springframework.security: INFO  # giảm noise
